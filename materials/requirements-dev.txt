# Development-only dependencies.
#
# The generated site and the prototypes have no runtime dependencies and must
# stay that way. This file exists solely so structural (JSON Schema) validation
# can run in development and CI.
#
# Semantic validation is pure standard library, so the test suite still passes
# without this installed; the structural tests skip instead of failing.

jsonschema==4.26.0
